name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:
 
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: |
          curl -s https://colorglassfortaleza.com.br > /dev/null
          curl -s -L https://colorglass.onrender.com/ > /dev/null

